# pidigi.env - place this on the SD card boot partition as /boot/pidigi.env
# Loaded by digi/bootstrap.sh on first boot to pre-seed direwolf.conf and service user

# Service user (created if missing)
USER=packet

# Station identity
CALLSIGN=KE8DCJ-1
LAT=47^15.00N
LON=088^27.00W
ALT=1260
COMMENT=AIOC Digi
SYMBOL=/r

# Audio device(s) (use names from aplay -l / arecord -l)
ADEVICE_RX=plughw:AllInOneCable,0
ADEVICE_TX=plughw:AllInOneCable,0
ARATE=48000
ACHANNELS=1

# PTT: choose exactly one; leave empty to disable (e.g., using VOX)
# Example for CM108 GPIO on hidraw1:
PTT_LINE="PTT CM108 /dev/hidraw1"
# Example for serial RTS/DTR using a stable symlink:
#PTT_LINE="PTT /dev/serial/by-id/usb-FTDI_FT232R_USB_UART_XXXX RTS"

# Beacon cadence (keep conservative for normal ops)
PBEACON_DELAY=30
PBEACON_EVERY=15
PBEACON_VIA="via=WIDE2-1"

# Overwrite an existing direwolf.conf if present
FORCE_CONFIG=1
